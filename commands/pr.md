---
description: Push current branch and create a pull request
argument-hint: "[title]"
allowed-tools: [Bash]
model: haiku
---
Run `${CLAUDE_PLUGIN_ROOT}/scripts/pr.sh $ARGUMENTS` and display the output. Do not add any commentary.
